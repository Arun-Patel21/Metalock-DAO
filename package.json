{
  "name": "metalock-dao",
  "version": "1.0.0",
  "description": "A decentralized governance DAO enabling proposal creation and voting through blockchain consensus.",
  "scripts": {
    "test": "npx hardhat test",
    "deploy": "npx hardhat run scripts/deploy.js --network sepolia"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "dotenv": "^16.0.3",
    "hardhat": "^2.22.1"
  }
}
